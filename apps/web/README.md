# @vora/web

Next.js 15 (App Router) PWA — rider + driver modes for VORA. See the [repo root README](../../README.md) for setup and the [build brief](../../docs/VORA-BUILD.md) for the full spec.

```bash
pnpm dev:web   # from repo root, or `pnpm dev` from this directory
```
