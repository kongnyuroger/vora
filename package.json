{
  "name": "vora",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "pnpm --filter @vora/web dev",
    "dev:api": "pnpm --filter @vora/api start:dev",
    "build": "pnpm -r build"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
